# Machine-specific configuration — NOT tracked in git.
#
# Written by `lara setup` on 2026-08-20 15:03:24. This file deep-merges over config.yaml,
# which holds portable defaults; anything true of *this computer* rather than of the
# project belongs here. Safe to delete — deleting it returns you to the defaults.
#
# Detected: Darwin/arm64, mps, 17 GB RAM
# Index:    torch int8 — 7.6 GB for 29.6M chunks, p50 ~8.2ms, recall 0.996
# Budget:   11 GB usable, 4.4 GB for models and cache
# Scoping:  required — see `lara corpus scope`

index:
  backend: torch
  precision: int8
  rerank:
    cross_encoder:
      enabled: true
hot_tier:
  max_bytes: 2000000000
disk:
  root: /Users/robertbedichek/Desktop/James/Local-arxiv-Research-Assistant/data
embedding:
  devices: auto
